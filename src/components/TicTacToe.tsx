import { useState } from 'react';

export default function TicTacToe() {
  function Input (text) {
    return(
      <div>ああ</div>
    );
  }

  return (
    <div className="App">
      <Input text="test"/>
      <div>追加</div>
    </div>
  );
}